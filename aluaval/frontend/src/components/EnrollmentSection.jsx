const EnrollmentSection = ({
  selectedStudents,
  availableStudents,
  studentSearch,
  onSelectStudent,
  onEnrollStudents,
  onStudentSearch,
  onBack,
}) => {
  return null;
};

export default EnrollmentSection;
